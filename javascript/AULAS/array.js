const alunos = ['Luiz', 'Maria', 'João']

// alunos.push ('Luiza')  Adiciona no fim
// alunos.unshift ('Fábio') adiciona no começo
// const removido = alunos.pop() Pop remove o último elemento que pode ser add em uma variável.
// const removido = alunos.shift() Shift remove o primeiro elemento.
// delete alunos[1]
alunos.push('Luiza', 'Eduardo')
console.log(alunos.slice(0, -2)) 